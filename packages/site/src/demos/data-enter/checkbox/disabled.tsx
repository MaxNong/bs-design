import React from 'react';
import { Checkbox } from 'eh-design';

export default () => (
  <>
    <Checkbox defaultChecked={false} disabled />
    <br />
    <Checkbox defaultChecked disabled />
  </>
);
